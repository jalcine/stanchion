[package]
name = "stanchion-abi"
description = "Runtime-agnostic types shared by every plugin backend"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde.workspace = true
semver.workspace = true
toml.workspace = true
mlua = { workspace = true, optional = true, default-features = false, features = ["lua54"] }

[features]
default = []
lua = ["dep:mlua"]

[lints]
workspace = true